To install the required packeges 
Got to telegram-msg-forward directory and type 'pip install -r tele_requirements.txt'

To confogure telegram message forwarder script got the telegram_conf.py and set it up!  ;)
;) ;)